// const menuBtn = document.querySelector('.menu__burger');


// const showMenu = () => {
//     document.querySelectorAll('.active').forEach(item => item.classList.toggle('show'));
// }

// menuBtn.addEventListener('click', showMenu)